// x.algo es una instancia de String

let x = 'Pepe'
x = x.toUpperCase();  //x se ha convertido en una instancia del objeto String.

// aDatos es una Instancia de Array. De todos los arrays este es uno concreto.

let aDatos = [1, 12, 67]

//Math no se instancia. Se usa como objeto global

// let z = new Math()  ->Esto esta mal.

Math.floor(34.98)
Math.ceil(34.98)
Math.round(34.25)

Math.random()  //genera un número aleatorio entre 0 y 1 que se llama semilla. Se usa mucho para generar azar. Ejemplo: para un juego.

parseInt(Math.random()*10) //pseudorandom entre 0 y 10
max(n1, n2)
min(n1, n2)

